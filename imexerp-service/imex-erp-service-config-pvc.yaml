apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: imexerp-config-pvc
  namespace: imexerp-prd
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 1Gi