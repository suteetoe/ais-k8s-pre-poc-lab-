apiVersion: apps/v1
kind: Deployment
metadata:
  name: imex-erp-stock-process-service
  labels:
    app: imex-erp-stock-process-service
  namespace: imexerp-prd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: imex-erp-stock-process-service
  template:
    metadata:
      labels:
        app: imex-erp-stock-process-service
    spec:
      containers:
        - name: imex-erp-stock-process-service
          image: ghcr.io/smlsoft/smljavawebservice:3.9.13-jdk11
          # resources:
          #   requests:
          #     memory: "4096Mi"
          #     cpu: "4000m"
          #   limits:
          #     memory: "8192Mi"
          #     cpu: "5000m" 
          volumeMounts:
            - name: config-volume
              mountPath: /etc/config
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /SMLJavaWebService/health
              port: 8080
              scheme: HTTP
            initialDelaySeconds: 90
          readinessProbe:
            httpGet:
              path: /SMLJavaWebService/health
              port: 8080
              scheme: HTTP
            initialDelaySeconds: 30
          envFrom:
          - secretRef:
              name: imex-erp-stock-process-service-secret
      volumes:
      - name: config-volume
        persistentVolumeClaim:
          claimName: imexerp-config-pvc  